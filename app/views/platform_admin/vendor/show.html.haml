%h1.black-text.center-align Update #{vendor.name}

.black-text
  = form_for(@vendor, url: vendor_path) do |f|
    = f.label :status
    = f.text_field :status
    .waves-effect.waves-light.btn
      = f.submit "Update Vendor"
